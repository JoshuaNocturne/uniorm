# 代码写法

- **变量名要读出它是什么，尽量少出现单字母名字。** 循环计数器、下标、临时列名也写成
  `index` / `slot` / `column` 这一类，而不是 `i` / `j` / `c`。模板形参名是例外：
  `T`、`M` 这一类照惯例保留。被改动的那几行里已有的单字母变量顺手换掉，不为一件小事
  整仓扫一遍。
- **手写格式，不跑 `clang-format`。** `.clang-format` 只是编辑器基线，流水线不校验
  格式；改完的代码按周围的样子手工对齐，每行不超过 80 列（commit 正文同此）。
- **注释默认不写。** 只在原因不显然时写一行：隐藏的约束、微妙的不变量、某个 bug 的绕法。
  不解释代码在做什么，也不写"用于 X 流程""为 issue#123 添加"这类会随代码腐烂的话；
  一行说得完的不要写成多段。
